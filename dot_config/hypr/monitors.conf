# Managed by chezmoi for MacBookPro8,2.
#
# The AMD boot path exposes the real internal panel as LVDS-1 and a ghost
# panel as LVDS-2. Keep the real 1440x900 panel at 1x scale and disable the
# ghost panel.

env = GDK_SCALE,1
monitor = LVDS-1,preferred,auto,1
monitor = LVDS-2,disable
